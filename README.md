# 📅 Event-Practice: Full-Stack Event Platform

Вебзастосунок для організації та пошуку івентів. Проєкт дозволяє користувачам створювати власні події (онлайн/офлайн), підписуватися на заходи інших учасників та фільтрувати івенти за категоріями та форматом.

---

## 🛠 Технологічний стек

| Сфера | Технології |
| :--- | :--- |
| **Frontend** | React (Vite), Redux Toolkit, React Router v6 |
| **Backend** | Node.js, Express.js |
| **Database** | PostgreSQL, Sequelize (ORM) |
| **Auth** | JWT, bcrypt (хешування паролів) |
| **Emailing** | Nodemailer (автоматизація привітань та сповіщень) |

---

## ✨ Ключовий функціонал

* **Автентифікація:** Реєстрація з валідацією дати народження та безпечний вхід.
* **Управління івентами:** Повний CRUD для власних подій (тип, локація/посилання, категорія).
* **Система участі (M:N):** Користувачі можуть записуватися на івенти інших авторів.
* **Динамічна фільтрація:** Пошук за категоріями, типом (Online/Offline) та датами через `searchParams`.
* **Background Tasks:** Автоматична розсилка привітань з днем народження через Nodemailer.

---

## 📊 Архітектура бази даних

Проєкт базується на реляційній структурі з чіткими зв'язками між сутностями.



* **Users 1:N Events:** Зв'язок автора з його івентами (`creatorId`).
* **Users M:N Events:** Участь користувачів у подіях через таблицю-посередник `Participations`.
* **Events N:1 Categories:** Класифікація подій за тематикою.

---

## 🚀 Запуск проєкту

### 1. Сервер (Backend)

```bash
cd server
npm install
# Налаштуйте .env (DB_NAME, DB_USER, DB_PASS)
npx sequelize db:create
npx sequelize db:migrate
npx sequelize db:seed:all
npm run dev
```

### 2. Клієнт (Frontend)

```bash
cd client
npm install
npm run dev
```

### 🛣 Основні API Ендпоінти

| Метод | Шлях | Опис |
| :--- | :--- | :--- |
| **GET** | `/events` | Отримати всі івенти з фільтрацією |
| **POST** | `/events` | Створити новий івент |
| **POST** | `/participations` | Записатися на івент |
| **GET** | `/users/:id/events` | Івенти користувача (створені/участь) |
| **DELETE** | `/events/:id` | Видалити власний івент |
